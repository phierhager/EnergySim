# EnergySim

EnergySim is a HEMS simulation framework written in JAX. It supports automatic MPC formulation using JAX computation graphs.

## Installation
Clone the repository and install dependencies:
```bash
git clone https://github.com/phierhager/EnergySim.git
cd EnergySim
pip install -e .
```

## Examples

Examples can be find in the directory examples/.